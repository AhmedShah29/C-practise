#include <stdio.h>

int main() {
    //its like js (every functions has its own scoope)
    //  in other words u can use the same var name in other functions
    // golpal vars are not in any function/s can be used in any fn
    // + hard to debug and also takes more memory

    return 0;
}