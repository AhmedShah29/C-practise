#include <stdio.h>
#include <math.h>

int main() {

    int x = 9;
    float y = 3.14;
    float a = 3;
    //x = sqrt(x); return 3
    //x = pow(x, 3); return x ^ 3 = 729
    //y = round(y); return 3.00...
    //y = ceil(y); returns 4.0...
    //y = floor(y); also retuns 3.0...
    //a = abs(a); retruns the destance to 0 witch is 3 
    //a = log(a);
    //x = sin(x);


    printf("%d", x);
    
    return 0;
}